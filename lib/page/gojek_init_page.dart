import 'package:flutter/material.dart';

class GojekInitPage extends StatelessWidget {
  const GojekInitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Gojek Page"),
      ),
      body: Container(
        child: Text("here..."),
      ),
    );
  }
}
