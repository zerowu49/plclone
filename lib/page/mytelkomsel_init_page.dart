import 'package:flutter/material.dart';

class MytelkomselInitPage extends StatelessWidget {
  const MytelkomselInitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Mytelkomsel Page"),
      ),
      body: Container(
        child: Text("here..."),
      ),
    );
  }
}
