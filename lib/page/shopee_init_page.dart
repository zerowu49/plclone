import 'package:flutter/material.dart';

class ShopeeInitPage extends StatelessWidget {
  const ShopeeInitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Shopee Page"),
      ),
      body: Container(
        child: Text("here..."),
      ),
    );
  }
}
